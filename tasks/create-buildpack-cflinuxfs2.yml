platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cflinuxfs2

params:
  PHP_5_VERSION:
  PHP_7_VERSION:

inputs:
- name: php-pipeline-source
- name: php-buildpack-source
- nane: php-binaries

outputs:
- name: php-buildpack-built

run:
  path: php-pipeline-source/tasks/create-buildpack-cflinuxfs2.sh
